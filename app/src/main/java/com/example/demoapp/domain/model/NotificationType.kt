package com.example.demoapp.domain.model

enum class NotificationType {
    LIKE, COMMENT, FOLLOWER, VERIFIED,
    NEW_PUBLICATION, REJECTED, REVIEW_REMINDER, ACHIEVEMENT,
    BAN, BAN_APPEAL, BAN_LIFTED
}